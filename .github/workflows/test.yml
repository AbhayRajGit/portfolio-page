name: Email Test Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 24f2002343@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Confirm run
        run: echo "Workflow executed successfully"
